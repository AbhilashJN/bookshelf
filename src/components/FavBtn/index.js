import React from 'react';
import PropTypes from 'prop-types';
import './favBtn.css';

class FavBtn extends React.Component {
  render() {
    return <button className="fav-btn" />;
  }
}
FavBtn.defaultProps = {
};
FavBtn.propTypes = {
};
export default FavBtn;
